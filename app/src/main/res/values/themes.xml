<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MoneyManager.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.MoneyManager</item>
    </style>

    <style name="Theme.MoneyManager" parent="android:Theme.Material.Light.NoActionBar" />

</resources>
