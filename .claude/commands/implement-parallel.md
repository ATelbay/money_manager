If a spec name is provided as argument ($ARGUMENTS), read /specs/$ARGUMENTS/tasks.md and plan.md.
If no argument is provided, find the most recently modified spec folder in .specify/specs/ and use that.

Tasks tagged with [P] can be executed in parallel.
Define the right amount of teammates and distribute tasks between them.
Ensure teammates won't touch the same files.
Use delegate mode.